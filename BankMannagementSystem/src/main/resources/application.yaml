server:
  port: 8084

spring:
  application:
    name: Bank

  datasource:
    url: jdbc:oracle:thin:@localhost:1521:xe
    username: SYSTEM
    password: omjee123
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

info:
  app:
    name: Bank Mannagement System
    version: "1.0"
    description: Spring Boot Student Project

management:
  endpoints:
    web:
      exposure:
        include: health,info
  info:
    env:
      enabled: true
